Implement the following plan:

# Fix: PendingPushRemote not cleared on early returns in handleTurnEndCondense

## Context

The reviewer agent found that `handleTurnEndCondense` has 4 early return paths that skip the push-and-clear block for `PendingPushRemote` (lines 1539-1550). The most important is the `!hasNew` return at line 1531 — when there's no new transcript content, the function returns before pushing or clearing the field. The stale value persists in session state until the next `InitializeSession` call.

## Approach: `defer` + extracted helper

Use `defer` at the top of `handleTurnEndCondense` to guarantee the push runs on all exit paths. Extract the push logic into `flushPendingPush` on `ManualCommitStrategy`, placed in `manual_commit_push.go` alongside `recordPendingPushRemote` (they form a pair: record and flush).

## Steps (TDD)

### 1. Write failing test

**File:** `cmd/entire/cli/strategy/manual_commit_push_test.go`

Add `TestHandleTurnEnd_PushClearedWhenNoNewContent`:
- Set up session, transition to ACTIVE_COMMITTED via PostCommit
- Record pending push remote
- Set `CheckpointTranscriptStart = 2` to make `sessionHasNewContent` return false
- Fire turn-end transition, call `HandleTurnEnd`
- Assert `PendingPushRemote` is empty

### 2. Run the test — verify it fails for the right reason

Run `mise run test` (or the specific test) and confirm:
- The test **fails** on the `PendingPushRemote` assertion
- `PendingPushRemote` is still `"origin"` (not empty), proving the early return at line 1531 skips the clear
- No other unexpected failures

Only proceed to the fix after confirming this.

### 3. Extract `flushPendingPush` helper

**File:** `cmd/entire/cli/strategy/manual_commit_push.go`

```go
func (s *ManualCommitStrategy) flushPendingPush(logCtx context.Context, state *SessionState) {
    if state.PendingPushRemote == "" {
        return
    }
    remote := state.PendingPushRemote
    state.PendingPushRemote = ""
    logging.Info(logCtx, "turn-end: pushing checkpoint data", slog.String("remote", remote))
    if pushErr := pushSessionsBranchCommon(remote, paths.MetadataBranchName); pushErr != nil {
        logging.Warn(logCtx, "turn-end: failed to push checkpoint data",
            slog.String("remote", remote), slog.String("error", pushErr.Error()))
    }
}
```

### 4. Add `defer` and remove inline block

**File:** `cmd/entire/cli/strategy/manual_commit_hooks.go`

- Add `defer s.flushPendingPush(logCtx, state)` as the first line of `handleTurnEndCondense` (line 1486, before `OpenRepository`)
- Remove the inline push block at lines 1539-1550

### 5. Run the test again — verify it passes

Run the specific test and confirm the previously-failing assertion now passes.

### 6. Run full CI checks

```bash
mise run fmt && mise run lint && mise run test:ci
```

## Files modified

| File | Change |
|------|--------|
| `cmd/entire/cli/strategy/manual_commit_push_test.go` | Add 1 test |
| `cmd/entire/cli/strategy/manual_commit_push.go` | Add `flushPendingPush` method |
| `cmd/entire/cli/strategy/manual_commit_hooks.go` | Add `defer`, remove inline push block |


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/danielweinmann/.REDACTED.jsonl

---

Awesome! Let's commit and push.